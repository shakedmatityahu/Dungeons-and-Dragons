package Units.Enemies;

public class Trap extends Enemy {
    protected Trap(char tile, String name, int healthCapacity, int attack, int defense) {
        super(tile, name, healthCapacity, attack, defense);
    }
}
