package Units.Enemies;

public class Trap extends Enemy {
    protected Trap(char tile, String name, int healthCapacity, int attack, int defense, int exprience) {
        super(tile, name, healthCapacity, attack, defense, exprience);
    }
}
